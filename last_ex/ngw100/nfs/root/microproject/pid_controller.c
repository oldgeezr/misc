#include <stdio.h>

int pid_controller (int y) {

	/*

	error = reference - y;
	integral = integral + (error * periode);
	derivative = (error - previous_error);
	previous_error = error;
	u = Kp * error + Ki * intergral + Kd * derivative;
	*/
	return 0;
}
